# TYPE  DATABASE        USER            ADDRESS                 METHOD
local   all             all                                     trust
host    all             all             127.0.0.1/32            scram-sha-256
host    all             all             ::1/128                 scram-sha-256
# Keycloak + admin from LAN and CRC networks
host    all             keycloak        192.168.0.0/24          scram-sha-256
host    all             keycloak        192.168.126.0/24        scram-sha-256
host    all             keycloak        192.168.130.0/24        scram-sha-256
host    all             keycloak        10.217.0.0/16           scram-sha-256
# CRC/podman pasta user-mode networking source seen from host DB
host    all             keycloak        169.254.0.0/16          scram-sha-256
host    all             postgres        192.168.0.0/24          scram-sha-256
host    all             postgres        169.254.0.0/16          scram-sha-256
# Streaming replication from Windows standby
host    replication     replicator      192.168.0.102/32        scram-sha-256
host    replication     replicator      192.168.0.0/24          scram-sha-256
# PoC fallback: allow keycloak from any (lab only)
host    all             keycloak        0.0.0.0/0               scram-sha-256
host    replication     replicator      0.0.0.0/0               scram-sha-256
